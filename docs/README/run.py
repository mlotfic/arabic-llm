# run.py
from script import temp
from config.paths import DATA_RAW

if __name__ == '__main__':
    temp.main()
# This script serves as the entry point to run the main function from the temp module in script package.
# It imports the temp module and calls its main function when executed.